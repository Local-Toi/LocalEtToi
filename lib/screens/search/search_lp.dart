import 'package:flutter/material.dart';

class SearchLP extends StatelessWidget {
  const SearchLP({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Search");
  }
}
